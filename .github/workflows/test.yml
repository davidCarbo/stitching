name: test
on:
  push:
    branches:
      - master
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up buildx
        uses: docker/setup-buildx-action@v1
      - name: Build Docker image
        run: docker buildx build --load -t apptest7 ./container_image_processing
      - name:
        run: echo "holi que tal"
